dossier du projet --> Digger
compte rendu
UML

GarbageDigger est l'executable du jeu

**important**


des libréries externes sont utilisé
pour compiler le code 
1) il faut ajouter ce qui est dans Libs\jars au "build path"
2) if faut ajouter le native au jar lwjgl (il existe 4 version du native dans Libs\lwjgl\native pour windows, mac, linux et meme solaris)

works with JDK 11 or above
